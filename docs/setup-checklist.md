# セットアップ残タスク整理（2026-07-15時点）

## ⏱️ ネクスト

**ボール = ①先輩（PR #6のマージ）／②RIZAP（トレーニング開始日の名簿への入力）**

1. 🔴 **PR #6のマージが必須（MTG前に完了させる）**：
   https://github.com/re-hatch-data/rizap-condition-check-ai/pull/6
   （CI green・コンフリクトなし・マージ可能。これがマージされないと`main`に今回の改良
   （Gemini移行・施策開始日連携・因果関係プロンプト等）が反映されず、MTG当日の
   `bootstrap.sh`実行が古い内容のままになる）
2. ⏳ RIZAPに「`00_被験者名簿`の『トレーニング開始日』列への入力」を依頼済み（Slack送付済み）。
   入力があり次第、次回実行分から自動的に開始前/開始後比較が有効になる（コード対応は完了済み）
3. ⏳ 事前準備: 正式なプロジェクトIDと、soxai-runner サービスアカウントの
   正式なメールアドレスを確認（`docs/iam-request.md` の①〜③）
4. ⏳ ミーティング当日、Editor権限を持つ担当が `deploy/bootstrap.sh <PROJECT_ID>` を実行
   （手順: `docs/runbook.md` 冒頭。OAuth・ブラウザ認可は不要）
5. ⏸ 構築後、E2E動作確認（bootstrap末尾の即時実行 → 実シートへの反映確認 →
   翌朝以降、再スタンプが機能しているか数日目視）

## 2026-07-14: 施策開始日の取得方式（実装済み）

**「00_被験者名簿」（マスター名簿）から取得する方式に最終決定・実装完了。**
当初検討した「被験者ごとに個別シートを追加してもらう案」は不採用（RIZAP側の入力の手間、
`被験者情報`シートの有無が被験者によってばらつく問題があったため）。

- 対象ファイル：`00_被験者名簿`スプレッドシート、シート名`被験者名簿`
  （`https://docs.google.com/spreadsheets/d/1guzsxOVhRVIAMQpWXnbESOO6h6BqOk9tu4huHLd-_4U`。
  `ROSTER_SHEET_ID`環境変数で上書き可、既定値はこのファイル）
- RIZAPには、この名簿の「トレーニング開始日」列（既に列は用意済み）に、
  被験者ごとに1行で入力してもらうよう依頼済み
- **各被験者スプレッドシートへの転記は不要**。名簿の`soxai_id`列（例: `rizap_024`）が
  各被験者のSOXAI_dailyの`ユーザーID`列と完全一致するため、本ジョブがこの名簿を
  1回読み込み、`soxai_id`をキーに突き合わせるだけで済む
- 実装：`src/sheets_client.py`の`load_training_start_dates`/`parse_training_start_dates`、
  `src/main.py`で被験者ごとに`ユーザーID`から辞書引きして`compute_flags`に渡す
- 名簿に行が無い/開始日が未入力の被験者は自動フォールバック（開始前/開始後比較なし・
  全履歴基準）で安全に動作。テスト追加済み（`tests/test_sheets_client.py`）

## 2026-07-11の追加改良（実装済み）

- [x] 比較軸を「前日比・7日平均比・全履歴SD逸脱度」から「施策開始前平均→開始後平均の変化
      ＋当日の開始後平均からのSD逸脱度」に変更
- [x] アンケート参照を常に前日分に統一（当日分の未提出ゆらぎを無くし、再生成ロジックを単純化）
- [x] 実行スケジュールを固定時刻（JST8:30）から、JST7:00〜8:55の5分おき起動に変更
      （SOXAI Ring同期の完了をデータ到着で自動検知。待機コード不要、コスト増もごく僅か）
- [x] **Geminiプロンプトへの「一般的な傾向」の追加（2026-07-15実装完了）**。
      調査した9候補のうち採用は4件（入眠潜時の平均・不眠の目安/就寝起床時刻の規則性(一般化)/
      睡眠とストレスの相互関係/歩数の目安）。以下は不採用：
      - 睡眠段階の割合・睡眠効率・SOXAIストレススコアの算出方式：確度中、単一の一次資料が
        特定できず不採用
      - WHO週150〜300分運動ガイドライン：確度は高いが、週単位・分単位の指標であり、
        本システムが持つ「日次・kcal単位」のデータと単位/期間が噛み合わないため不採用
      - 就寝/起床時刻の規則性の具体的閾値（社会的ジェットラグの「2時間」等）：引用元研究が
        平日/休日の差を測定したものであり、本人の期間平均からの当日ズレとは測定対象が異なる
        ため、具体的数値は使わず一般化した表現のみ採用
      すべて「断定的な基準・診断として扱わない、あくまで傾向」という枠組みでシステムプロンプトに
      テキストとして追加（`src/comment_generator.py`の`REFERENCE_TENDENCIES`）。判定ロジックへの
      組み込みは無し。テスト追加済み

## A. REHATCH側で今すぐできること（RIZAPからの返答を待たずに）

- [x] リポジトリ実装（`src/`一式・テスト・CI・deployスクリプト・ドキュメント）
- [x] `COND_FOLDER_ID` の確認（`rizap-soxai-ring/setup.sh` から確認済み）
- [ ] OAuthクライアント作成・`scripts/generate_oauth_token.py` の動作確認
      （GCPプロジェクトが無くても、個人のGCPプロジェクトで動作自体は確認可能）

## B. 構築当日の流れ

```
1. (RIZAP) 実行担当アカウントへ rizap-marketing のEditor権限付与、SA名の確認
2. (合同)  deploy/bootstrap.sh <PROJECT_ID> を実行
   - 内部で setup_gcp.sh(API有効化・IAM付与) → SAキーのSecret登録 → デプロイ
   - IAM付与が失敗する場合は (RIZAP) のIAM管理者に依頼
   - 最後に即時1回実行まで自動で行う
3. (合同) 実シートでの動作確認 → 本番運用開始
```

## 運用開始前に決めること（PoC後でも可）

- [x] **「適正値」の基準：個人内基準のみで確定（2026-07-16）**。実装は既に本人の施策開始後
      平均からのSD逸脱度のみで判定しており、チーム/全体比較のロジックは実装していない。
      追加のデータ・実装は不要、この前提で運用開始してよい
- [x] **SD逸脱度の閾値：初期値2.0のまま運用開始し、実データを見ながら事後調整する方針に決定
      （2026-07-16）**。変更は`gcloud run jobs update condition-check-ai --update-env-vars
      SD_THRESHOLD=<値>`の軽量なコマンドで反映でき、`deploy.sh`によるフルデプロイは不要なため、
      MTG前の事前検証（実データアクセスが無いと不可能）は必須ではないと判断
- [ ] Agent Platform（旧Vertex AI）経由のGeminiモデルの料金感の確認
